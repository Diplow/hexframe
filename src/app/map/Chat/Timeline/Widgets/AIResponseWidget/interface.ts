/**
 * Public API for AIResponseWidget Subsystem
 * 
 * Consumers: Widget renderers, chat system
 */

// Main AI Response Widget
export { AIResponseWidget } from './index';

/**
 * AIResponseWidget Subsystem Public Interface
 * 
 * Handles display of AI job processing states and responses with
 * real-time feedback, progress indication, and error handling.
 * 
 * Components are exported above for direct usage.
 */