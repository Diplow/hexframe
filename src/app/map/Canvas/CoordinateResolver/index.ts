/**
 * CoordinateResolver - Virtual composition coordinate abstraction
 *
 * Provides clean API for resolving virtual composition containers
 * (coordinates ending in ,0 that don't exist as database items).
 */

export { CoordinateResolver } from "~/app/map/Canvas/CoordinateResolver/coordinate-resolver";
