// Legacy drop targets calculator removed - now handled by DOM-based drag service
// Note: These exports remain for backward compatibility but functionality moved to DOMBasedDragService

export function isValidDropTarget() {
  return false; // Stub - DOM service handles this now
}

export function getValidDropTargets() {
  return []; // Stub - DOM service handles this now
}

export function getDropOperationType() {
  return null; // Stub - DOM service handles this now
}