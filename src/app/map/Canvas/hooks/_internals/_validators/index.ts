// Legacy drag permission validator removed - now handled by DOM-based drag service
// Stub exports for backward compatibility

export function canDragTile() {
  return true; // DOM service handles validation internally
}

export function canSwapTiles() {
  return true; // DOM service handles validation internally
}