import type { ComposedContext } from '../../types'

export class NarrativeContextSerializer {
  serialize(_context: ComposedContext): string {
    // TODO: Implement narrative serialization
    // This would provide a more human-readable, story-like format
    return 'Narrative format not yet implemented'
  }
}